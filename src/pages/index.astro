---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import Hero from '../components/Hero.astro';
---
<BaseLayout title="The Batch — A Sunday cooking club in Barcelona">
  <Nav />
  <Hero />
  {/*
    Still to port from index.html (in order):
      - HowMealPrep  (the Graza 3-card section)
      - Pricing      (Solo / Pair / Table of 4, early-bird banner)
      - PhoneCarousel (past batches)
      - HowItWorks   (4 steps w/ SVG sketches)
      - KitchenStrip (horizontal scroll-snap photo strip)
      - Story        (Ralu's bio)
      - Testimonials (3 cards, middle featured)
      - Footer       (big CTA, ink background)
  */}
</BaseLayout>
